<div class='tweets'>
  <% @tweets.each do |tweet| %>
    <div class='tweet'>
      <p><%= tweet.body %><p>
      <p><%= tweet.user.email %></p>
      <p><%= time_ago_in_words tweet.created_at %> ago</p>
    </div>
  <% end %>
</div>